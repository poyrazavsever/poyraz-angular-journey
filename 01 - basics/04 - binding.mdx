---
title: "Binding Nedir?"
description: "Geldik Binding konusuna. Angular'da veri bağlama nasıl yapılır, ne tür veri bağlama yöntemleri vardır, hepsini anlatıyorum."
date: "2025-12-05"
author: "Poyraz Avsever"
level: "beginner"
category: "Angular Basics"
tags:
  - angular
  - topic
  - learning
  - guide
  - data-binding
  - event-binding
---

> Navigasyon: [Yol haritası](../00%20-%20roadmap/roadmap.mdx) · Önceki: [Angular Nedir?](./03%20-%20angular.mdx) · Sonraki: [Todo App Rehberi (first-app)](./first-app/about-project.mdx)

### Binding Nedir?

Binding, bir uygulamadaki veri ile kullanıcı arayüzü (UI) bileşenleri arasında bağlantı kurma işlemidir. Angular'da binding, verilerin bileşenler ve HTML şablonları arasında senkronize edilmesini sağlar. Tatlı değil mi 🤠?

### Angular'da Binding Türleri

Angular'da dört ana binding türü vardır:

- Event Binding (Olay Bağlama)
- Interpolation (Ara Değerleme)
- Property Binding (Özellik Bağlama)
- Two-Way Binding (Çift Yönlü Bağlama)

#### 1. Event Binding (Olay Bağlama)

Event Binding, kullanıcı etkileşimlerini (örneğin, tıklamalar, klavye girişleri) bileşenlere bağlamak için kullanılır. Bu, kullanıcı eylemlerine yanıt vermek için idealdir. Aynı commit içersinde örneği var.

#### 2. Interpolation (Ara Değerleme)

Interpolation, bileşen verilerini HTML şablonunda görüntülemek için kullanılır. Örneğin, bir bileşen özelliğini HTML içinde göstermek için çift süslü parantezler kullanılır: `{{ propertyName }}`.

#### 3. Property Binding (Özellik Bağlama)

Property Binding, HTML elementlerinin özelliklerini bileşen verilerine bağlamak için kullanılır. Örneğin, bir butonun `disabled` özelliğini bir bileşen özelliesine bağlayabilirsiniz: `<button [disabled]="isDisabled">Click Me</button>`.

#### 4. Two-Way Binding (Çift Yönlü Bağlama)

Two-Way Binding, bileşen verileri ile HTML elementleri arasında çift yönlü veri akışını sağlar. Bu, kullanıcı girişlerinin bileşen verilerini otomatik olarak güncellemesini sağlar. Angular'da bu, `ngModel` direktifi kullanılarak yapılır:

```html
<input [(ngModel)]="propertyName" />
```

### Özet

Binding, Angular uygulamalarında veri ve kullanıcı arayüzü bileşenleri arasında etkileşim kurmanın temel yoludur. Event Binding, Interpolation, Property Binding ve Two-Way Binding gibi farklı yöntemler, uygulamanızın ihtiyaçlarına göre veri akışını yönetmenizi sağlar. Bu yöntemleri doğru kullanarak, dinamik ve etkileşimli web uygulamaları geliştirebilirsiniz.
